# Issue7 successor contract for14 and10

Publication preparation only. Firstmate publishes this substance after reviewed issue7
integration; this does not advance14/15/10 or claim issue7 delivery complete.

## Accepted API and executed ownership

Init/Std-only namespace StrictLeanPolicy retains `accept p roles s`, `accept_iff`,
`accept_sound`, `accept_complete`, `accepted_report_identity`, `accepted_covers_slot`.
`ResultState.collect s inputs` executes every insertion without filtering or overwrite.
`collect_success_iff` characterizes `BatchOK` (distinct required keys, initial vacancy,
exact binding); `collect_empty_lookup` connects supplied inputs to final entries.
`finalize p roles inputs : Except FinalizationFailure (Finalized p roles inputs)` calls
that collector and `accept`. `finalize_iff` gives exact success iff `InputsOK`; the
specification requires every independently planned slot and every approved policy.
`finalized_covers_input` returns the actual supplied policy-satisfying observation for
any required slot. It does not assume that a producer returned the complete list.
`AcceptedRun c` packages census/plan/roles/inputs/finalized at the exact claim index;
its `.report` is the accepted report projection. `CombinedAccepted pc dc documents`
retains fresh project and exact-documentation certificates under the same snapshot;
`combineAccepted`, `combined_policy`, `combined_reports_same_snapshot` govern composition.
No JSON field deserializes to proof authority.

Operational owners: Checker.Acceptance.freeze/finish (project, incremental, explicit
file); Documentation.freezeDocuments/finishDocuments/auditBuiltProject (fences);
FreshChecker.freezeGraph/finishGraph (optional serialized graph). AxiomGate's combined
parent joins its surface child, decodes request-bound raw SurfaceProduction, reconciles
expected modules with its own Lake assignments, recomputes policy, then combines accepted
packages. RuleExamples.documentation retains its documentation package and exact original
request. ResultProtocol.writeAccepted requires AcceptedRun, not raw AcceptedReport.
Source-backed complete success/non-audit inventory: docs/guides/policy-acceptance.md §1.
Elaborated signatures and axiom account: session/evidence/issue-7-signatures.{lean,txt}.

## Modes and retained boundaries

Fresh project means isolated claimed-source build plus full policy checks. Incremental
project/build-lint keeps Lake's incremental artifacts but reruns current policy; it makes
no fresh whole-project claim. Fresh file checks the requested bytes through an explicit
FileSourceBinding to the actual temporary compiler URI; dependencies remain incremental.
No-profile/compiler-trusting file results are CLASSIFIED without a conforming certificate.
Documentation freezes original Markdown and fence byte spans before work. Positive,
compiler-negative and teaching fences retain separate meanings; policy-negative example
production/qualification remains its existing separate typed diagnostic path. All four
accepted example kinds remain; INCOMPLETE demonstrations discharge none. Empty declared
modules and no-fence documents still require their discovery/build/scan jobs. No empty
worker response can narrow the requested census. Serialized graph is a separate optional
claim: Census.graphRoots/graphCoverage are frozen before worker results; plan-only has no
certificate. Help/internal workers/editor snapshots/qualifiers are explicitly non-audit.

Census retains full replay module and required/admitted declaration keys. The narrow
InfrastructureOrigin/InfrastructureOK partition authenticates exact canonical reporter,
codec and forced-collector artifacts and incoming imports; it creates no whole-library
exemption. Contract/Diagnostic/StructuralName remain public interfaces; ordinary positive
imports of Probe/Report/PolicyCodec are refused. Declared excluded modules remain excluded.
Module/source mappings, execution closures, supported toolchain, source/configuration
freshness, worker request/slot identities, strict codecs and terminal checks remain.

## Commands, evidence and remaining obligations

Use pinned lean-toolchain/lake-manifest.json; `lake build` is the development loop.
The complete ordinary acceptance remains `./scripts/verify.sh`, cold root and hard420s,
without grace/override or composing separate inner runs. Setup may provision pinned
dependency artifacts first. Selected packet diagnostics use
`python3 scripts/acceptance_checks.py --group surface|evidence|fences|process --evidence PATH`;
these are execution-linkage fault controls supplementing universal proofs, not acceptance.
Required CI diagnostics now run `./scripts/verify.sh diagnostics producers`, then
`./scripts/verify.sh diagnostics history`, sequentially with independent hard420 limits.
Their combined allowance is up to840; full 21+17 public invocations and 8+17 transport
controls/order are retained. The former combined420 failure remains a failure. Ordinary
cold420, rule-example corpus, site and optional serialized-graph meanings are unchanged.
See issue-7-diagnostic-budget.md for the resource-contract change and pending evidence.
Local focused results, failures and exact input identities are in issue-7-verification.md;
The first build-lint diagnostic exceeded its180s pilot window; subsequent unchanged
full build-lint and policy-domain campaigns passed under separate420s diagnostic bounds
in282.68s and220.03s. These do not establish cold-root acceptance. Final signed head,
cold420, independent proof/integration review and exact-head hosted
receipts must be filled by the delivery pipeline before this handoff is published.

Proofs establish exact finite coverage and approved predicates on supplied observations.
They do not authenticate Lean/Lake extraction, file/source identity, JSON parsing, freshness
between observations, scheduler/signals, compiled machine code or contract semantic adequacy.
Dependency Git dirty/path state is observed and bound to the request, not a kernel proof
of the filesystem or a fresh dependency rebuild. Runtime pointer equality retains a full
structural fallback; renderer strings/JSON are not universally proved. Keep every residual
R-* account and local/global distinction. No whole IO-checker verification claim.

Con-leche CheckedRecord/collectChecks/FullyChecked/checkDeclsIO architecture is credited
to its authors/contributors, maintained by Joachim Breitner at Lean FRO. No con-leche
imports. Optional8/9 do not gate core10; delivery order remains7,14,15,10. No release,
visibility change, second parser/typechecker/server or plugin marketplace.
